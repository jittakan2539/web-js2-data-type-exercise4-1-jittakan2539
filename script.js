const Products = {
  product_id,
  product_name,
  quantity_in_stock,
  unit_price,
  product_description: {
    product_brand,
    size_or_quantity,
    taste_or_scent,
    colour,
    additional_description,
  },
};

console.log(Products.product_id);
